[project]
name = "cicd-demo-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "black>=24.0.0",
    "flake8>=7.0.0",
    "flask>=3.0.0",
    "gunicorn>=21.0.0",
    "pytest>=8.0.0",
    "pytest-cov>=5.0.0",
]
